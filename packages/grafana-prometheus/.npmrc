# TODO: Re-evaluate once @grafana/ui is bumped to a stable v13 release.
# Required because @grafana/assistant -> @grafana/scenes -> react-router-dom@6
# conflicts with @grafana/ui -> react-router-dom@5. If v13 completes the
# react-router v6 migration, this may no longer be necessary.
legacy-peer-deps=true
