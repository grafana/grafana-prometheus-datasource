name: Crowdin automatic task management

on:
  workflow_dispatch:
  # Disabled until existing translations have been bulk-imported into Crowdin.
  # Pair with Josh Hunt before enabling. Uncomment the schedule below when ready.
  # schedule:
  #   - cron: "0 0 1 * *"

jobs:
  create-tasks-in-crowdin:
    uses: grafana/grafana-github-actions/.github/workflows/crowdin-create-tasks.yml@main
    with:
      crowdin_project_id: 0 # TODO: replace with the project ID from the crowdin-create-project workflow output
