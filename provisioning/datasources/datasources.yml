apiVersion: 1

datasources:
  - name: 'prometheus'
    uid: 'prometheus'
    type: 'prometheus'
    access: proxy
    url: 'http://prometheus:9090'
    isDefault: false
    orgId: 1
    version: 1
    editable: true
    jsonData:
      httpMethod: 'GET'
